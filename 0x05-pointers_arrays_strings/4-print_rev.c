#include "main.h"

/*
* function to print string in reverse order
*/
void print_rev(char *s)
{
    int i;

    for(i = 0; s[i] !=0; i++)
    {

    }

    for(i = i - 1; i >= o; i--)
    {
        _putchar(i);
    }
    _putchar('\n');
}